sudo: required
dist: trusty
before_install:
    - sudo apt-get -qq update && sudo apt-get install -y --no-install-recommends texlive-fonts-recommended texlive-latex-extra texlive-latex-recommended texlive-lang-french ghc haskell-platform
install:
    - cabal update
    - cabal install pandoc pandoc-crossref
    - pip install -U pip pandocfilters
cache:
    - pip

        # - os: linux
        #   sudo: false
        #   language: r
        #   pandoc_version: 2.1
        #   addons:
        #     apt:
        #       packages:
        #         - texlive-fonts-recommended texlive-latex-extra texlive-latex-recommended texlive-lang-french cabal-install ghc haskell-platform
        #   install:
        #       - cabal update
        #       - cabal install pandoc-crossref
        #       - pip install --user pandocfilters pandoc-numbering
        #   cache:
        #   - packages
        #   - pip
script:
    - make 
# deploy:
#   provider: releases
#   api_key: "$GH_TOKEN"
#   file:
#     - "cours.pdf"
#   skip_cleanup: true
#   on:
#     tags: true
